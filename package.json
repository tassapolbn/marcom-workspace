{
  "name": "marcom-workspace",
  "version": "2.0.0",
  "private": true,
  "description": "HeadStart International School Phuket - MARCOM department workspace (Netlify + Firebase)",
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.35.1"
  },
  "scripts": {
    "test:ui": "node --test tests/ui-*.test.mjs",
    "preview:ui": "node tests/ui-preview.mjs",
    "test:rules": "firebase emulators:exec --only firestore \"node tests/rules.test.mjs\""
  }
}
